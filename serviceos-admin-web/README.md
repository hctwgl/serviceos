# ServiceOS Admin Web（M101～M130）

总部运营后台：队列/目录/详情/工作区/命令，以及创建项目、修订范围、分配候选、表单/资料/预约/上门全命令。

```bash
npm install && npm run dev
npm run build
```

明确未实现：设计系统、SavedView、正式 OIDC、Network/Technician、SERVICE-only 车企适配层 UI、E2E。
