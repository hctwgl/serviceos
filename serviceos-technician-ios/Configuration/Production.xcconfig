#include "Base.xcconfig"
SERVICEOS_ENV = production
SERVICEOS_API_BASE_URL = $(SERVICEOS_PRODUCTION_API_BASE_URL)
SERVICEOS_OIDC_ISSUER = $(SERVICEOS_PRODUCTION_OIDC_ISSUER)
SWIFT_OPTIMIZATION_LEVEL = -O
