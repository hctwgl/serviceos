# ADR-005：配置继承在发布时扁平化

- 状态：Proposed
- 日期：2026-07-12

## 背景

项目需要从标准模板继承并局部覆盖，但如果运行时沿多层继承链动态解析，模板更新可能改变历史行为，故障解释和性能也会变复杂。

## 决策

继承和覆盖只用于草稿编辑。发布前解析完整继承链、检测冲突，并生成扁平、不可变的 `PublishedVersion`。运行时只读取该发布版本，不动态追踪父模板。

## 后果

- 历史行为确定，运行时简单且可缓存；
- 发布版本体积可能增加；
- 模板升级需三方差异与显式合并；
- 必须保留来源链和最终字段来源，支持解释和升级。
