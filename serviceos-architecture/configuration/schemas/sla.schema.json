{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://serviceos.local/schemas/sla-v1.schema.json",
  "title": "ServiceOS Task SLA Policy",
  "type": "object",
  "additionalProperties": false,
  "required": ["policyKey", "version", "subjectType", "taskTypes", "startEvent", "stopEvent", "clockMode", "targetDurationSeconds"],
  "properties": {
    "policyKey": {"type": "string", "pattern": "^[a-z0-9][a-z0-9._-]*$", "maxLength": 128},
    "version": {"type": "string", "pattern": "^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)(?:-[0-9A-Za-z.-]+)?$"},
    "subjectType": {"const": "TASK"},
    "taskTypes": {
      "type": "array",
      "minItems": 1,
      "maxItems": 100,
      "uniqueItems": true,
      "items": {"type": "string", "minLength": 1, "maxLength": 100}
    },
    "startEvent": {"const": "TASK_CREATED"},
    "stopEvent": {"const": "TASK_COMPLETED"},
    "clockMode": {"enum": ["ELAPSED", "BUSINESS"]},
    "targetDurationSeconds": {"type": "integer", "minimum": 1, "maximum": 31536000},
    "calendarRef": {"type": "string", "pattern": "^[a-z0-9][a-z0-9._-]*$", "maxLength": 128}
  },
  "allOf": [
    {
      "if": {"properties": {"clockMode": {"const": "BUSINESS"}}, "required": ["clockMode"]},
      "then": {"required": ["calendarRef"]}
    },
    {
      "if": {"properties": {"clockMode": {"const": "ELAPSED"}}, "required": ["clockMode"]},
      "then": {"not": {"required": ["calendarRef"]}}
    }
  ]
}
